apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: werify-uploads
  namespace: werify-staging
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-static
  volumeName: werify-staging-uploads
  resources:
    requests:
      storage: 100Gi
